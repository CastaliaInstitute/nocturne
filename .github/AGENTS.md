# Language and Tooling Policy

- Preferred stack for this repository is implementation-agnostic documentation-first
  development until platform code appears.
- Use Markdown for architecture, policy, and operational notes.
- Use deterministic, testable implementations for runtime modules.
- Keep repository defaults consistent (`.gitignore`, line endings, UTF-8).
- Use local-first assumptions by default for new design and API decisions.
