# Kotlin Binding Scaffold

- JNI/FFI boundary is intentionally placeholder.
- Implement `nocturne-sdk` bridge module in native build chain.
- Keep API version checks aligned with `nocturne_sdk_api.h`.
