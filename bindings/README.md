# Nocturne Language Binding Scaffolds

This directory contains generated/placeholder scaffolds for downstream bindings.

- `cpp/` — C++ header exposure and build integration notes.
- `rust/` — Rust FFI scaffold.
- `kotlin/` — Kotlin Multiplatform/Android-facing API draft.
